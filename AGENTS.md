# Project Memory

- 称呼用户为 bon。
- 与用户沟通时使用中文。
- 修改完项目后，直接执行 git commit 并 git push，除非用户另有说明或存在需要用户确认的阻塞。
- 每次推送时，同步更新 Web 首页人生时钟下方的本版改动文案，用 6 个字及以内描述本次更改内容。
- Web 界面默认只显示结果、字段标签、状态和必要操作，不显示算法、计算口径或实现原理等解释性文字，除非用户明确要求。
